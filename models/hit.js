var express = require('express');
var router = express.Router();
var cards = require('./models/cards');


//this will be the function of the hit button
var hitBtn = function() {
    //deal one more card to the players hand
    //if the hand is over 21 and there are no spilt the hand is over
};


module.exports = hitBtn;